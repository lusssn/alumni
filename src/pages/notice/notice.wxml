<view class="padding-t-md">
  <view class="margin-l-xl margin-r-xl">
    <Loading wx:if="{{!messageList}}" />
    <NoticeItem wx:key="messageId" wx:for="{{messageList}}" data="{{item}}" bindtap="handleClickMessage"/>
  </view>
</view>
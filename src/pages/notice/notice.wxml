<view class="container padding-t-md">
  <view class="card">
    <view class="card-block">
      <NoticeItem
        wx:for="{{noticeList}}"
        wx:key="messageId"
        data="{{item}}"
        data-id="{{item.fromUser}}"
        bindtap="handleClickCard"
      />
    </view>
  </view>
</view>

<view class="container padding-t-md">
  <view class="card">
    <view class="card-block">
      <NoticeItem
        wx:for="{{noticeList}}"
        wx:key="time"
        data="{{item}}"
        data-id="{{item.friendid}}"
        bindtap="handleClickCard"
      />
    </view>
  </view>
</view>

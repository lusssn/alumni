<view class="container padding-t-md padding-l-xl padding-r-xl">
  <BusinessCard
    wx:for="{{list}}"
    wx:key="*this"
    data-id="{{item.accountId}}"
    bindtap="handleClickCard"
    data="{{item}}"
  />
</view>
